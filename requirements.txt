# Floors, not pins — the pipeline is version-tolerant within these ranges.
# Last verified end-to-end 2026-07-31 on Python 3.14.2 with the versions in brackets.
datasets>=4.0     # [5.0.1]
pandas>=2.0       # [3.0.5]
numpy>=2.0,<3     # [2.5.1]  major-version capped: Generator stream stability is not
                  #          guaranteed across NumPy majors (see README caveats)
scipy>=1.13       # [1.18.0]
pyarrow>=15.0     # [25.0.0]
